#!/bin/bash

if [[ -d ./jdk25 ]]
then
  rm -rf ./jdk25
fi

echo Downloading java...
mkdir "jdk25"
curl -s -L "https://corretto.aws/downloads/resources/25.0.0.36.2/amazon-corretto-25.0.0.36.2-@os@-@arch@.tar.gz" | tar -vxz -C jdk25 --strip-components=1
